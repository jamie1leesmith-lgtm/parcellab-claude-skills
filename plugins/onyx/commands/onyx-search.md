---
description: Semantic search across your Onyx knowledge base and pull the results into context
---

Use the `onyx_search` tool to search the Onyx knowledge base for: **$ARGUMENTS**

Then:
1. Summarise the most relevant findings.
2. Cite each source with its title and link.
3. If a result looks important but the snippet is incomplete, use `onyx_fetch_document` with its `document_id` to pull the full text before answering.
