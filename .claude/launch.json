{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "layout-preview",
      "runtimeExecutable": "python3",
      "runtimeArgs": [
        "-c",
        "import sys; sys.path = [p for p in sys.path if p]; import http.server, socketserver, functools; H = functools.partial(http.server.SimpleHTTPRequestHandler, directory='/Users/jamie.lee-smith/parcellab-previews'); socketserver.TCPServer(('127.0.0.1', 8098), H).serve_forever()"
      ],
      "port": 8098
    }
  ]
}
